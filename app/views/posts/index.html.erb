<div class="navbar">
  <% if user_signed_in? %>
    Welcome! <%= current_user.username%>
  <% end %>
</div>
<%= link_to 'New post', new_post_path%>
<h1>Timeline</h1>
<% @posts.each do |post| %>
  <%= post.title %>
  <%= post.body %>
  <%= post.user.username %>
<% end %>